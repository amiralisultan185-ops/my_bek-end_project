# Migrations

POWER LAW Digital uses Prisma Migrate. The executable migration history is stored in:

```text
prisma/migrations/
```

This directory exists because the final submission checklist asks for a top-level `migrations/` entry. Do not duplicate or manually edit Prisma migration SQL here.
